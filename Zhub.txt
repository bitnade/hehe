-- [[ CLEANUP PREVIOUS ]] --
pcall(function()
    if cleardrawings then cleardrawings() end
end)

if _G.ZHubFOVCircle then
    pcall(function() _G.ZHubFOVCircle:Remove() end)
    pcall(function() _G.ZHubFOVCircle:Destroy() end)
    _G.ZHubFOVCircle = nil
end

local function PurgePreviousGUIs(parent)
    if not parent then return end
    for _, v in pairs(parent:GetChildren()) do
        if v:IsA("ScreenGui") and (v.Name == "MovementPro" or v.Name == "ZHub" or string.find(v.Name, "Movement_Elite")) then
            pcall(function() v:Destroy() end)
        end
    end
    for _, v in pairs(parent:GetDescendants()) do
        if v.Name == "FOVCircleGui" or v.Name == "ZHubFOVCircle" then
            pcall(function() v:Destroy() end)
        end
    end
end

PurgePreviousGUIs(game:GetService("CoreGui"))
if pcall(function() return gethui() end) and gethui() then
    PurgePreviousGUIs(gethui())
end
if game:GetService("Players").LocalPlayer and game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui") then
    PurgePreviousGUIs(game:GetService("Players").LocalPlayer.PlayerGui)
end

local CurrentLoopID = tick()
_G.MovementLoopID = CurrentLoopID

-- [[ SERVICES ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local Camera = workspace.CurrentCamera

-- [[ THEME ]] --
local Theme = {
    Background = Color3.fromRGB(12, 12, 16),
    Surface = Color3.fromRGB(18, 18, 24),
    SurfaceHover = Color3.fromRGB(24, 24, 32),
    Accent = Color3.fromRGB(99, 102, 241),
    AccentGlow = Color3.fromRGB(129, 140, 248),
    Success = Color3.fromRGB(34, 197, 94),
    Danger = Color3.fromRGB(239, 68, 68),
    Warning = Color3.fromRGB(251, 191, 36),
    Text = Color3.fromRGB(250, 250, 250),
    TextMuted = Color3.fromRGB(148, 148, 160),
    Border = Color3.fromRGB(38, 38, 48),
    GradientStart = Color3.fromRGB(99, 102, 241),
    GradientEnd = Color3.fromRGB(168, 85, 247),
}

-- [[ LIGHTING CACHE ]] --
local LightCache = {
    Brightness = Lighting.Brightness,
    ClockTime = Lighting.ClockTime,
    FogEnd = Lighting.FogEnd,
    GlobalShadows = Lighting.GlobalShadows,
    OutdoorAmbient = Lighting.OutdoorAmbient
}

-- [[ GLOBAL STATES ]] --
_G.Noclip, _G.Fly, _G.SpeedEnabled, _G.EspEnabled, _G.InfJump, _G.FreeCam, _G.FullBright = false, false, false, false, false, false, false
_G.ReachEnabled = false
_G.KillAura = false
_G.AutoAim = false
_G.ShowFOV = true
_G.AimFOV = 150
_G.AimSmoothness = 0.25
_G.AimPart = "Head"
_G.AimWallCheck = true
_G.HeadshotChance = 0.40
_G.SmoothAim = false  -- false = Direct Aim (snap), true = Smooth Aim (lerp)
_G.TeamCheck = true   -- true = skip teammates in auto aim
_G.AutoShoot = false  -- true = automatically fire at locked/valid target
_G.MovementSpeed, _G.SafeMode, _G.TimeScale = 50, true, 1
_G.ReachDistance = 15
_G.KillAuraRange = 20
_G.ESPRange = 1000
_G.SelectedPlayer = ""
_G.IgnoredPlayers = _G.IgnoredPlayers or {}

-- [[ CONFIG / STATE SAVE SYSTEM ]] --
local HttpService = game:GetService("HttpService")
local CONFIG_FILE = "ZHub_FOV_Config.json"

local function SaveConfig()
    pcall(function()
        if writefile then
            local data = {
                ShowFOV = _G.ShowFOV,
                AimFOV = _G.AimFOV,
                AimSmoothness = _G.AimSmoothness,
                HeadshotChance = _G.HeadshotChance,
                SmoothAim = _G.SmoothAim,
                TeamCheck = _G.TeamCheck,
                AutoShoot = _G.AutoShoot,
                ESPRange = _G.ESPRange,
                ReachDistance = _G.ReachDistance,
                KillAuraRange = _G.KillAuraRange,
                MovementSpeed = _G.MovementSpeed
            }
            writefile(CONFIG_FILE, HttpService:JSONEncode(data))
        end
    end)
end

local function LoadConfig()
    pcall(function()
        if readfile and isfile and isfile(CONFIG_FILE) then
            local content = readfile(CONFIG_FILE)
            local data = HttpService:JSONDecode(content)
            if type(data) == "table" then
                if data.ShowFOV ~= nil then _G.ShowFOV = data.ShowFOV end
                if data.AimFOV ~= nil then _G.AimFOV = data.AimFOV end
                if data.AimSmoothness ~= nil then _G.AimSmoothness = data.AimSmoothness end
                if data.HeadshotChance ~= nil then _G.HeadshotChance = data.HeadshotChance end
                if data.SmoothAim ~= nil then _G.SmoothAim = data.SmoothAim end
                if data.TeamCheck ~= nil then _G.TeamCheck = data.TeamCheck end
                if data.AutoShoot ~= nil then _G.AutoShoot = data.AutoShoot end
                if data.ESPRange ~= nil then _G.ESPRange = data.ESPRange end
                if data.ReachDistance ~= nil then _G.ReachDistance = data.ReachDistance end
                if data.KillAuraRange ~= nil then _G.KillAuraRange = data.KillAuraRange end
                if data.MovementSpeed ~= nil then _G.MovementSpeed = data.MovementSpeed end
            end
        end
    end)
end

LoadConfig()

-- [[ UI UTILITIES ]] --
local function CreateTween(obj, props, duration, style, direction)
    return TweenService:Create(obj, TweenInfo.new(duration or 0.2, style or Enum.EasingStyle.Quart, direction or Enum.EasingDirection.Out), props)
end

local function ApplyCorner(obj, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius or 8)
    corner.Parent = obj
    return corner
end

local function ApplyStroke(obj, color, thickness, transparency)
    local stroke = Instance.new("UIStroke")
    stroke.Color = color or Theme.Border
    stroke.Thickness = thickness or 1
    stroke.Transparency = transparency or 0
    stroke.Parent = obj
    return stroke
end

local function ApplyGradient(obj, startColor, endColor, rotation)
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, startColor or Theme.GradientStart),
        ColorSequenceKeypoint.new(1, endColor or Theme.GradientEnd)
    })
    gradient.Rotation = rotation or 45
    gradient.Parent = obj
    return gradient
end

local function ApplyPadding(obj, padding)
    local pad = Instance.new("UIPadding")
    pad.PaddingTop = UDim.new(0, padding)
    pad.PaddingBottom = UDim.new(0, padding)
    pad.PaddingLeft = UDim.new(0, padding)
    pad.PaddingRight = UDim.new(0, padding)
    pad.Parent = obj
    return pad
end

-- [[ MAIN UI ]] --
local parentContainer = nil
if pcall(function() return gethui() end) and gethui() then
    parentContainer = gethui()
elseif LocalPlayer and LocalPlayer:FindFirstChild("PlayerGui") then
    parentContainer = LocalPlayer.PlayerGui
else
    parentContainer = game:GetService("CoreGui")
end

-- Cleanup old ZHub instances in parentContainer safely without destroying game controls
if parentContainer then
    for _, child in pairs(parentContainer:GetChildren()) do
        if child.Name == "ZHub" then
            pcall(function() child:Destroy() end)
        end
    end
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ZHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = parentContainer

-- Main Container
local Main = Instance.new("Frame")
Main.Name = "Main"
Main.BackgroundColor3 = Theme.Background
Main.Position = UDim2.new(0.5, -180, 0.5, -240)
Main.Size = UDim2.new(0, 360, 0, 480)
Main.Parent = ScreenGui
ApplyCorner(Main, 12)
ApplyStroke(Main, Theme.Border, 1)

-- Glow Effect
local Glow = Instance.new("ImageLabel")
Glow.Name = "Glow"
Glow.BackgroundTransparency = 1
Glow.Position = UDim2.new(0.5, 0, 0, -20)
Glow.AnchorPoint = Vector2.new(0.5, 0)
Glow.Size = UDim2.new(0, 200, 0, 60)
Glow.Image = "rbxassetid://5028857084"
Glow.ImageColor3 = Theme.Accent
Glow.ImageTransparency = 0.7
Glow.Parent = Main

-- Header
local Header = Instance.new("Frame")
Header.Name = "Header"
Header.BackgroundColor3 = Theme.Surface
Header.Size = UDim2.new(1, 0, 0, 50)
Header.Parent = Main
ApplyCorner(Header, 12)

-- Fix bottom corners of header
local HeaderFix = Instance.new("Frame")
HeaderFix.BackgroundColor3 = Theme.Surface
HeaderFix.Position = UDim2.new(0, 0, 1, -12)
HeaderFix.Size = UDim2.new(1, 0, 0, 12)
HeaderFix.BorderSizePixel = 0
HeaderFix.Parent = Header

-- Logo/Brand
local Logo = Instance.new("TextLabel")
Logo.Name = "Logo"
Logo.Parent = Header
Logo.BackgroundTransparency = 1
Logo.Position = UDim2.new(0, 16, 0, 0)
Logo.Size = UDim2.new(0, 120, 1, 0)
Logo.Font = Enum.Font.GothamBlack
Logo.Text = "Z"
Logo.TextColor3 = Theme.Text
Logo.TextSize = 18
Logo.TextXAlignment = Enum.TextXAlignment.Left

local LogoAccent = Instance.new("TextLabel")
LogoAccent.Name = "LogoAccent"
LogoAccent.Parent = Header
LogoAccent.BackgroundTransparency = 1
LogoAccent.Position = UDim2.new(0, 32, 0, 0) 
LogoAccent.Size = UDim2.new(0, 40, 1, 0)
LogoAccent.Font = Enum.Font.GothamBlack
LogoAccent.Text = "HUB"
LogoAccent.TextColor3 = Theme.Accent
LogoAccent.TextSize = 18
LogoAccent.TextXAlignment = Enum.TextXAlignment.Left

-- Session Timer
local SessionLabel = Instance.new("TextLabel")
SessionLabel.Name = "Session"
SessionLabel.BackgroundTransparency = 1
SessionLabel.Position = UDim2.new(0.5, -40, 0, 0)
SessionLabel.Size = UDim2.new(0, 80, 1, 0)
SessionLabel.Font = Enum.Font.GothamMedium
SessionLabel.Text = "00:00:00"
SessionLabel.TextColor3 = Theme.TextMuted
SessionLabel.TextSize = 11
SessionLabel.Parent = Header

-- Window Controls
local Controls = Instance.new("Frame")
Controls.Name = "Controls"
Controls.BackgroundTransparency = 1
Controls.Position = UDim2.new(1, -80, 0, 0)
Controls.Size = UDim2.new(0, 70, 1, 0)
Controls.Parent = Header

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Name = "Minimize"
MinimizeBtn.BackgroundColor3 = Theme.SurfaceHover
MinimizeBtn.Position = UDim2.new(0, 0, 0.5, -12)
MinimizeBtn.Size = UDim2.new(0, 24, 0, 24)
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.Text = "−"
MinimizeBtn.TextColor3 = Theme.TextMuted
MinimizeBtn.TextSize = 16
MinimizeBtn.Parent = Controls
ApplyCorner(MinimizeBtn, 6)

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "Close"
CloseBtn.BackgroundColor3 = Theme.Danger
CloseBtn.BackgroundTransparency = 0.8
CloseBtn.Position = UDim2.new(0, 34, 0.5, -12)
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "×"
CloseBtn.TextColor3 = Theme.Danger
CloseBtn.TextSize = 16
CloseBtn.Parent = Controls
ApplyCorner(CloseBtn, 6)

-- Content Area
local Content = Instance.new("ScrollingFrame")
Content.Name = "Content"
Content.BackgroundTransparency = 1
Content.Position = UDim2.new(0, 0, 0, 55)
Content.Size = UDim2.new(1, 0, 1, -55)
Content.ScrollBarThickness = 3
Content.ScrollBarImageColor3 = Theme.Accent
Content.ScrollBarImageTransparency = 0.5
Content.AutomaticCanvasSize = Enum.AutomaticSize.Y
Content.CanvasSize = UDim2.new(0, 0, 0, 1250)
Content.Parent = Main
ApplyPadding(Content, 12)

local ContentLayout = Instance.new("UIListLayout")
ContentLayout.Padding = UDim.new(0, 10)
ContentLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ContentLayout.Parent = Content

-- [[ INPUT SECTION ]] --
local InputSection = Instance.new("Frame")
InputSection.Name = "InputSection"
InputSection.BackgroundTransparency = 1
InputSection.Size = UDim2.new(1, 0, 0, 45)
InputSection.Parent = Content

local InputLayout = Instance.new("UIListLayout")
InputLayout.FillDirection = Enum.FillDirection.Horizontal
InputLayout.Padding = UDim.new(0, 10)
InputLayout.Parent = InputSection

local function CreateInputBox(name, placeholder, width, parentFrame)
    local Container = Instance.new("Frame")
    Container.Name = name
    Container.BackgroundColor3 = Theme.Surface
    Container.Size = UDim2.new(0, width, 0, 40)
    Container.Parent = parentFrame or InputSection
    ApplyCorner(Container, 8)
    ApplyStroke(Container, Theme.Border, 1) 

    local Icon = Instance.new("TextLabel")
    Icon.BackgroundTransparency = 1
    Icon.Position = UDim2.new(0, 12, 0, 0)
    Icon.Size = UDim2.new(0, 20, 1, 0)
    Icon.Font = Enum.Font.GothamBold
    Icon.Text = name == "Speed" and "⚡" or (name == "KillAuraRange" and "💀" or (name == "AimFOV" and "🎯" or "⏱"))
    Icon.TextColor3 = Theme.Accent
    Icon.TextSize = 14
    Icon.Parent = Container

    local Input = Instance.new("TextBox")
    Input.Name = "Input"
    Input.BackgroundTransparency = 1
    Input.Position = UDim2.new(0, 35, 0, 0)
    Input.Size = UDim2.new(1, -45, 1, 0)
    Input.Font = Enum.Font.GothamMedium
    Input.PlaceholderText = placeholder
    Input.Text = ""
    Input.TextColor3 = Theme.Text
    Input.PlaceholderColor3 = Theme.TextMuted
    Input.TextSize = 12
    Input.TextXAlignment = Enum.TextXAlignment.Left
    Input.ClearTextOnFocus = false
    Input.Parent = Container

    return Input, Container
end

local SpeedInput, SpeedContainer = CreateInputBox("Speed", "Speed: 50", 155, InputSection)
local TimeInput, TimeContainer = CreateInputBox("Time", "Time: 1x", 155, InputSection)

SpeedInput.Text = "50"
TimeInput.Text = "1x"

-- [[ REACH INPUT SECTION ]] --
local ReachInputSection = Instance.new("Frame")
ReachInputSection.Name = "ReachInputSection"
ReachInputSection.BackgroundTransparency = 1
ReachInputSection.Size = UDim2.new(1, 0, 0, 45)
ReachInputSection.Parent = Content

local ReachInputLayout = Instance.new("UIListLayout")
ReachInputLayout.FillDirection = Enum.FillDirection.Horizontal
ReachInputLayout.Padding = UDim.new(0, 10)
ReachInputLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ReachInputLayout.Parent = ReachInputSection

local ReachContainer = Instance.new("Frame")
ReachContainer.Name = "Reach"
ReachContainer.BackgroundColor3 = Theme.Surface
ReachContainer.Size = UDim2.new(0, 320, 0, 40)
ReachContainer.Parent = ReachInputSection
ApplyCorner(ReachContainer, 8)
ApplyStroke(ReachContainer, Theme.Border, 1)

local ReachIcon = Instance.new("TextLabel")
ReachIcon.BackgroundTransparency = 1
ReachIcon.Position = UDim2.new(0, 12, 0, 0)
ReachIcon.Size = UDim2.new(0, 20, 1, 0)
ReachIcon.Font = Enum.Font.GothamBold
ReachIcon.Text = "🗡"
ReachIcon.TextColor3 = Theme.Accent
ReachIcon.TextSize = 14
ReachIcon.Parent = ReachContainer

local ReachLabel = Instance.new("TextLabel")
ReachLabel.BackgroundTransparency = 1
ReachLabel.Position = UDim2.new(0, 35, 0, 0)
ReachLabel.Size = UDim2.new(0, 80, 1, 0)
ReachLabel.Font = Enum.Font.GothamMedium
ReachLabel.Text = "Reach:"
ReachLabel.TextColor3 = Theme.TextMuted
ReachLabel.TextSize = 12
ReachLabel.TextXAlignment = Enum.TextXAlignment.Left
ReachLabel.Parent = ReachContainer

local ReachInput = Instance.new("TextBox")
ReachInput.Name = "Input"
ReachInput.BackgroundTransparency = 1
ReachInput.Position = UDim2.new(0, 115, 0, 0)
ReachInput.Size = UDim2.new(0, 60, 1, 0)
ReachInput.Font = Enum.Font.GothamMedium
ReachInput.PlaceholderText = "15"
ReachInput.Text = "15"
ReachInput.TextColor3 = Theme.Text
ReachInput.PlaceholderColor3 = Theme.TextMuted
ReachInput.TextSize = 12
ReachInput.TextXAlignment = Enum.TextXAlignment.Left
ReachInput.ClearTextOnFocus = false
ReachInput.Parent = ReachContainer

local ReachUnit = Instance.new("TextLabel")
ReachUnit.BackgroundTransparency = 1
ReachUnit.Position = UDim2.new(0, 175, 0, 0)
ReachUnit.Size = UDim2.new(0, 60, 1, 0)
ReachUnit.Font = Enum.Font.GothamMedium
ReachUnit.Text = "studs"
ReachUnit.TextColor3 = Theme.TextMuted
ReachUnit.TextSize = 11
ReachUnit.TextXAlignment = Enum.TextXAlignment.Left
ReachUnit.Parent = ReachContainer

-- [[ KILL AURA RANGE INPUT SECTION ]] --
local KillAuraInputSection = Instance.new("Frame")
KillAuraInputSection.Name = "KillAuraInputSection"
KillAuraInputSection.BackgroundTransparency = 1
KillAuraInputSection.Size = UDim2.new(1, 0, 0, 45)
KillAuraInputSection.Parent = Content

local KillAuraInputLayout = Instance.new("UIListLayout")
KillAuraInputLayout.FillDirection = Enum.FillDirection.Horizontal
KillAuraInputLayout.Padding = UDim.new(0, 10)
KillAuraInputLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
KillAuraInputLayout.Parent = KillAuraInputSection

local KillAuraContainer = Instance.new("Frame")
KillAuraContainer.Name = "KillAuraRange"
KillAuraContainer.BackgroundColor3 = Theme.Surface
KillAuraContainer.Size = UDim2.new(0, 320, 0, 40)
KillAuraContainer.Parent = KillAuraInputSection
ApplyCorner(KillAuraContainer, 8)
ApplyStroke(KillAuraContainer, Theme.Border, 1)

local KillAuraIcon = Instance.new("TextLabel")
KillAuraIcon.BackgroundTransparency = 1
KillAuraIcon.Position = UDim2.new(0, 12, 0, 0)
KillAuraIcon.Size = UDim2.new(0, 20, 1, 0)
KillAuraIcon.Font = Enum.Font.GothamBold
KillAuraIcon.Text = "💀"
KillAuraIcon.TextColor3 = Theme.Accent
KillAuraIcon.TextSize = 14
KillAuraIcon.Parent = KillAuraContainer

local KillAuraRangeLabel = Instance.new("TextLabel")
KillAuraRangeLabel.BackgroundTransparency = 1
KillAuraRangeLabel.Position = UDim2.new(0, 35, 0, 0)
KillAuraRangeLabel.Size = UDim2.new(0, 80, 1, 0)
KillAuraRangeLabel.Font = Enum.Font.GothamMedium
KillAuraRangeLabel.Text = "Aura Range:"
KillAuraRangeLabel.TextColor3 = Theme.TextMuted
KillAuraRangeLabel.TextSize = 12
KillAuraRangeLabel.TextXAlignment = Enum.TextXAlignment.Left
KillAuraRangeLabel.Parent = KillAuraContainer

local KillAuraRangeInput = Instance.new("TextBox")
KillAuraRangeInput.Name = "Input"
KillAuraRangeInput.BackgroundTransparency = 1
KillAuraRangeInput.Position = UDim2.new(0, 115, 0, 0)
KillAuraRangeInput.Size = UDim2.new(0, 60, 1, 0)
KillAuraRangeInput.Font = Enum.Font.GothamMedium
KillAuraRangeInput.PlaceholderText = "20"
KillAuraRangeInput.Text = "20"
KillAuraRangeInput.TextColor3 = Theme.Text
KillAuraRangeInput.PlaceholderColor3 = Theme.TextMuted
KillAuraRangeInput.TextSize = 12
KillAuraRangeInput.TextXAlignment = Enum.TextXAlignment.Left
KillAuraRangeInput.ClearTextOnFocus = false
KillAuraRangeInput.Parent = KillAuraContainer

local KillAuraRangeUnit = Instance.new("TextLabel")
KillAuraRangeUnit.BackgroundTransparency = 1
KillAuraRangeUnit.Position = UDim2.new(0, 175, 0, 0)
KillAuraRangeUnit.Size = UDim2.new(0, 60, 1, 0)
KillAuraRangeUnit.Font = Enum.Font.GothamMedium
KillAuraRangeUnit.Text = "studs"
KillAuraRangeUnit.TextColor3 = Theme.TextMuted
KillAuraRangeUnit.TextSize = 11
KillAuraRangeUnit.TextXAlignment = Enum.TextXAlignment.Left
KillAuraRangeUnit.Parent = KillAuraContainer

-- [[ ESP RANGE INPUT SECTION ]] --
local ESPRangeInputSection = Instance.new("Frame")
ESPRangeInputSection.Name = "ESPRangeInputSection"
ESPRangeInputSection.BackgroundTransparency = 1
ESPRangeInputSection.Size = UDim2.new(1, 0, 0, 45)
ESPRangeInputSection.Parent = Content

local ESPRangeInputLayout = Instance.new("UIListLayout")
ESPRangeInputLayout.FillDirection = Enum.FillDirection.Horizontal
ESPRangeInputLayout.Padding = UDim.new(0, 10)
ESPRangeInputLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ESPRangeInputLayout.Parent = ESPRangeInputSection

local ESPRangeContainer = Instance.new("Frame")
ESPRangeContainer.Name = "ESPRange"
ESPRangeContainer.BackgroundColor3 = Theme.Surface
ESPRangeContainer.Size = UDim2.new(0, 320, 0, 40)
ESPRangeContainer.Parent = ESPRangeInputSection
ApplyCorner(ESPRangeContainer, 8)
ApplyStroke(ESPRangeContainer, Theme.Border, 1)

local ESPRangeIcon = Instance.new("TextLabel")
ESPRangeIcon.BackgroundTransparency = 1
ESPRangeIcon.Position = UDim2.new(0, 12, 0, 0)
ESPRangeIcon.Size = UDim2.new(0, 20, 1, 0)
ESPRangeIcon.Font = Enum.Font.GothamBold
ESPRangeIcon.Text = "👁"
ESPRangeIcon.TextColor3 = Theme.Accent
ESPRangeIcon.TextSize = 14
ESPRangeIcon.Parent = ESPRangeContainer

local ESPRangeLabel = Instance.new("TextLabel")
ESPRangeLabel.BackgroundTransparency = 1
ESPRangeLabel.Position = UDim2.new(0, 35, 0, 0)
ESPRangeLabel.Size = UDim2.new(0, 90, 1, 0)
ESPRangeLabel.Font = Enum.Font.GothamMedium
ESPRangeLabel.Text = "ESP Range:"
ESPRangeLabel.TextColor3 = Theme.TextMuted
ESPRangeLabel.TextSize = 12
ESPRangeLabel.TextXAlignment = Enum.TextXAlignment.Left
ESPRangeLabel.Parent = ESPRangeContainer

local ESPRangeInput = Instance.new("TextBox")
ESPRangeInput.Name = "Input"
ESPRangeInput.BackgroundTransparency = 1
ESPRangeInput.Position = UDim2.new(0, 125, 0, 0)
ESPRangeInput.Size = UDim2.new(0, 60, 1, 0)
ESPRangeInput.Font = Enum.Font.GothamMedium
ESPRangeInput.PlaceholderText = "1000"
ESPRangeInput.Text = "1000"
ESPRangeInput.TextColor3 = Theme.Text
ESPRangeInput.PlaceholderColor3 = Theme.TextMuted
ESPRangeInput.TextSize = 12
ESPRangeInput.TextXAlignment = Enum.TextXAlignment.Left
ESPRangeInput.ClearTextOnFocus = false
ESPRangeInput.Parent = ESPRangeContainer

local ESPRangeUnit = Instance.new("TextLabel")
ESPRangeUnit.BackgroundTransparency = 1
ESPRangeUnit.Position = UDim2.new(0, 185, 0, 0)
ESPRangeUnit.Size = UDim2.new(0, 60, 1, 0)
ESPRangeUnit.Font = Enum.Font.GothamMedium
ESPRangeUnit.Text = "studs"
ESPRangeUnit.TextColor3 = Theme.TextMuted
ESPRangeUnit.TextSize = 11
ESPRangeUnit.TextXAlignment = Enum.TextXAlignment.Left
ESPRangeUnit.Parent = ESPRangeContainer

-- [[ AIM FOV SLIDER SECTION ]] --
local AimFOVSliderFrame = Instance.new("Frame")
AimFOVSliderFrame.Name = "AimFOVSliderFrame"
AimFOVSliderFrame.BackgroundColor3 = Theme.Surface
AimFOVSliderFrame.Size = UDim2.new(0, 320, 0, 50)
AimFOVSliderFrame.Parent = Content
ApplyCorner(AimFOVSliderFrame, 8)
ApplyStroke(AimFOVSliderFrame, Theme.Border, 1)

local AimFOVIcon = Instance.new("TextLabel")
AimFOVIcon.BackgroundTransparency = 1
AimFOVIcon.Position = UDim2.new(0, 12, 0, 6)
AimFOVIcon.Size = UDim2.new(0, 20, 0, 20)
AimFOVIcon.Font = Enum.Font.GothamBold
AimFOVIcon.Text = "🎯"
AimFOVIcon.TextColor3 = Theme.Accent
AimFOVIcon.TextSize = 14
AimFOVIcon.Parent = AimFOVSliderFrame

local AimFOVTitle = Instance.new("TextLabel")
AimFOVTitle.BackgroundTransparency = 1
AimFOVTitle.Position = UDim2.new(0, 35, 0, 6)
AimFOVTitle.Size = UDim2.new(0, 120, 0, 20)
AimFOVTitle.Font = Enum.Font.GothamMedium
AimFOVTitle.Text = "Aim FOV Slider"
AimFOVTitle.TextColor3 = Theme.TextMuted
AimFOVTitle.TextSize = 12
AimFOVTitle.TextXAlignment = Enum.TextXAlignment.Left
AimFOVTitle.Parent = AimFOVSliderFrame

local AimFOVInput = Instance.new("TextBox")
AimFOVInput.Name = "Input"
AimFOVInput.BackgroundTransparency = 1
AimFOVInput.Position = UDim2.new(1, -75, 0, 6)
AimFOVInput.Size = UDim2.new(0, 60, 0, 20)
AimFOVInput.Font = Enum.Font.GothamBold
AimFOVInput.Text = tostring(_G.AimFOV) .. " px"
AimFOVInput.TextColor3 = Theme.Accent
AimFOVInput.TextSize = 12
AimFOVInput.TextXAlignment = Enum.TextXAlignment.Right
AimFOVInput.ClearTextOnFocus = false
AimFOVInput.Parent = AimFOVSliderFrame

-- Track Line
local AimFOVTrack = Instance.new("Frame")
AimFOVTrack.Name = "Track"
AimFOVTrack.BackgroundColor3 = Theme.SurfaceHover
AimFOVTrack.Position = UDim2.new(0, 12, 0, 32)
AimFOVTrack.Size = UDim2.new(1, -24, 0, 6)
AimFOVTrack.Parent = AimFOVSliderFrame
ApplyCorner(AimFOVTrack, 3)

-- Fill Bar
local AimFOVFill = Instance.new("Frame")
AimFOVFill.Name = "Fill"
AimFOVFill.BackgroundColor3 = Theme.Accent
local minFOV, maxFOV = 10, 600
local initialPct = math.clamp((_G.AimFOV - minFOV) / (maxFOV - minFOV), 0, 1)
AimFOVFill.Size = UDim2.new(initialPct, 0, 1, 0)
AimFOVFill.Parent = AimFOVTrack
ApplyCorner(AimFOVFill, 3)

-- Knob / Handle
local AimFOVKnob = Instance.new("Frame")
AimFOVKnob.Name = "Knob"
AimFOVKnob.BackgroundColor3 = Theme.Text
AimFOVKnob.AnchorPoint = Vector2.new(0.5, 0.5)
AimFOVKnob.Position = UDim2.new(initialPct, 0, 0.5, 0)
AimFOVKnob.Size = UDim2.new(0, 14, 0, 14)
AimFOVKnob.Parent = AimFOVTrack
ApplyCorner(AimFOVKnob, 7)
ApplyStroke(AimFOVKnob, Theme.Accent, 2)

local fovDragging = false

local function UpdateFOVSlider(input)
    local trackPos = AimFOVTrack.AbsolutePosition.X
    local trackWidth = AimFOVTrack.AbsoluteSize.X
    local mouseX = input.Position.X
    local pct = math.clamp((mouseX - trackPos) / trackWidth, 0, 1)

    local val = math.floor(minFOV + (maxFOV - minFOV) * pct)
    _G.AimFOV = val
    AimFOVFill.Size = UDim2.new(pct, 0, 1, 0)
    AimFOVKnob.Position = UDim2.new(pct, 0, 0.5, 0)
    AimFOVInput.Text = tostring(val) .. " px"
    SaveConfig()
end

AimFOVTrack.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        fovDragging = true
        UpdateFOVSlider(input)
    end
end)


-- [[ HEADSHOT RATE SLIDER SECTION ]] --
local HeadshotSliderFrame = Instance.new("Frame")
HeadshotSliderFrame.Name = "HeadshotSliderFrame"
HeadshotSliderFrame.BackgroundColor3 = Theme.Surface
HeadshotSliderFrame.Size = UDim2.new(0, 320, 0, 50)
HeadshotSliderFrame.Parent = Content
ApplyCorner(HeadshotSliderFrame, 8)
ApplyStroke(HeadshotSliderFrame, Theme.Border, 1)

local HeadshotIcon = Instance.new("TextLabel")
HeadshotIcon.BackgroundTransparency = 1
HeadshotIcon.Position = UDim2.new(0, 12, 0, 6)
HeadshotIcon.Size = UDim2.new(0, 20, 0, 20)
HeadshotIcon.Font = Enum.Font.GothamBold
HeadshotIcon.Text = "💀"
HeadshotIcon.TextColor3 = Theme.Danger
HeadshotIcon.TextSize = 14
HeadshotIcon.Parent = HeadshotSliderFrame

local HeadshotTitle = Instance.new("TextLabel")
HeadshotTitle.BackgroundTransparency = 1
HeadshotTitle.Position = UDim2.new(0, 35, 0, 6)
HeadshotTitle.Size = UDim2.new(0, 130, 0, 20)
HeadshotTitle.Font = Enum.Font.GothamMedium
HeadshotTitle.Text = "Headshot Rate"
HeadshotTitle.TextColor3 = Theme.TextMuted
HeadshotTitle.TextSize = 12
HeadshotTitle.TextXAlignment = Enum.TextXAlignment.Left
HeadshotTitle.Parent = HeadshotSliderFrame

local HeadshotValueLabel = Instance.new("TextLabel")
HeadshotValueLabel.Name = "Value"
HeadshotValueLabel.BackgroundTransparency = 1
HeadshotValueLabel.Position = UDim2.new(1, -75, 0, 6)
HeadshotValueLabel.Size = UDim2.new(0, 60, 0, 20)
HeadshotValueLabel.Font = Enum.Font.GothamBold
HeadshotValueLabel.Text = tostring(math.floor(_G.HeadshotChance * 100)) .. "%"
HeadshotValueLabel.TextColor3 = Theme.Danger
HeadshotValueLabel.TextSize = 12
HeadshotValueLabel.TextXAlignment = Enum.TextXAlignment.Right
HeadshotValueLabel.Parent = HeadshotSliderFrame

local HeadshotTrack = Instance.new("Frame")
HeadshotTrack.Name = "Track"
HeadshotTrack.BackgroundColor3 = Theme.SurfaceHover
HeadshotTrack.Position = UDim2.new(0, 12, 0, 32)
HeadshotTrack.Size = UDim2.new(1, -24, 0, 6)
HeadshotTrack.Parent = HeadshotSliderFrame
ApplyCorner(HeadshotTrack, 3)

local HeadshotFill = Instance.new("Frame")
HeadshotFill.Name = "Fill"
HeadshotFill.BackgroundColor3 = Theme.Danger
local hsInitialPct = math.clamp(_G.HeadshotChance, 0, 1)
HeadshotFill.Size = UDim2.new(hsInitialPct, 0, 1, 0)
HeadshotFill.Parent = HeadshotTrack
ApplyCorner(HeadshotFill, 3)

local HeadshotKnob = Instance.new("Frame")
HeadshotKnob.Name = "Knob"
HeadshotKnob.BackgroundColor3 = Theme.Text
HeadshotKnob.AnchorPoint = Vector2.new(0.5, 0.5)
HeadshotKnob.Position = UDim2.new(hsInitialPct, 0, 0.5, 0)
HeadshotKnob.Size = UDim2.new(0, 14, 0, 14)
HeadshotKnob.Parent = HeadshotTrack
ApplyCorner(HeadshotKnob, 7)
ApplyStroke(HeadshotKnob, Theme.Danger, 2)

local headshotDragging = false

local function UpdateHeadshotSlider(input)
    local trackPos = HeadshotTrack.AbsolutePosition.X
    local trackWidth = HeadshotTrack.AbsoluteSize.X
    local mouseX = input.Position.X
    local pct = math.clamp((mouseX - trackPos) / trackWidth, 0, 1)
    _G.HeadshotChance = pct
    HeadshotFill.Size = UDim2.new(pct, 0, 1, 0)
    HeadshotKnob.Position = UDim2.new(pct, 0, 0.5, 0)
    HeadshotValueLabel.Text = tostring(math.floor(pct * 100)) .. "%"
end

HeadshotTrack.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        headshotDragging = true
        UpdateHeadshotSlider(input)
    end
end)

-- [[ AIM SMOOTHNESS SLIDER SECTION ]] --
local SmoothnessSliderFrame = Instance.new("Frame")
SmoothnessSliderFrame.Name = "SmoothnessSliderFrame"
SmoothnessSliderFrame.BackgroundColor3 = Theme.Surface
SmoothnessSliderFrame.Size = UDim2.new(0, 320, 0, 50)
SmoothnessSliderFrame.Parent = Content
ApplyCorner(SmoothnessSliderFrame, 8)
ApplyStroke(SmoothnessSliderFrame, Theme.Border, 1)

local SmoothnessIcon = Instance.new("TextLabel")
SmoothnessIcon.BackgroundTransparency = 1
SmoothnessIcon.Position = UDim2.new(0, 12, 0, 6)
SmoothnessIcon.Size = UDim2.new(0, 20, 0, 20)
SmoothnessIcon.Font = Enum.Font.GothamBold
SmoothnessIcon.Text = "🎚"
SmoothnessIcon.TextColor3 = Theme.Warning
SmoothnessIcon.TextSize = 14
SmoothnessIcon.Parent = SmoothnessSliderFrame

local SmoothnessTitle = Instance.new("TextLabel")
SmoothnessTitle.BackgroundTransparency = 1
SmoothnessTitle.Position = UDim2.new(0, 35, 0, 6)
SmoothnessTitle.Size = UDim2.new(0, 140, 0, 20)
SmoothnessTitle.Font = Enum.Font.GothamMedium
SmoothnessTitle.Text = "Aim Smoothness"
SmoothnessTitle.TextColor3 = Theme.TextMuted
SmoothnessTitle.TextSize = 12
SmoothnessTitle.TextXAlignment = Enum.TextXAlignment.Left
SmoothnessTitle.Parent = SmoothnessSliderFrame

local SmoothnessValueLabel = Instance.new("TextLabel")
SmoothnessValueLabel.Name = "Value"
SmoothnessValueLabel.BackgroundTransparency = 1
SmoothnessValueLabel.Position = UDim2.new(1, -75, 0, 6)
SmoothnessValueLabel.Size = UDim2.new(0, 60, 0, 20)
SmoothnessValueLabel.Font = Enum.Font.GothamBold
SmoothnessValueLabel.Text = string.format("%.2f", _G.AimSmoothness)
SmoothnessValueLabel.TextColor3 = Theme.Warning
SmoothnessValueLabel.TextSize = 12
SmoothnessValueLabel.TextXAlignment = Enum.TextXAlignment.Right
SmoothnessValueLabel.Parent = SmoothnessSliderFrame

local SmoothnessTrack = Instance.new("Frame")
SmoothnessTrack.Name = "Track"
SmoothnessTrack.BackgroundColor3 = Theme.SurfaceHover
SmoothnessTrack.Position = UDim2.new(0, 12, 0, 32)
SmoothnessTrack.Size = UDim2.new(1, -24, 0, 6)
SmoothnessTrack.Parent = SmoothnessSliderFrame
ApplyCorner(SmoothnessTrack, 3)

local minSmooth, maxSmooth = 0.01, 1.0
local smInitialPct = math.clamp((_G.AimSmoothness - minSmooth) / (maxSmooth - minSmooth), 0, 1)

local SmoothnessFill = Instance.new("Frame")
SmoothnessFill.Name = "Fill"
SmoothnessFill.BackgroundColor3 = Theme.Warning
SmoothnessFill.Size = UDim2.new(smInitialPct, 0, 1, 0)
SmoothnessFill.Parent = SmoothnessTrack
ApplyCorner(SmoothnessFill, 3)

local SmoothnessKnob = Instance.new("Frame")
SmoothnessKnob.Name = "Knob"
SmoothnessKnob.BackgroundColor3 = Theme.Text
SmoothnessKnob.AnchorPoint = Vector2.new(0.5, 0.5)
SmoothnessKnob.Position = UDim2.new(smInitialPct, 0, 0.5, 0)
SmoothnessKnob.Size = UDim2.new(0, 14, 0, 14)
SmoothnessKnob.Parent = SmoothnessTrack
ApplyCorner(SmoothnessKnob, 7)
ApplyStroke(SmoothnessKnob, Theme.Warning, 2)

local smoothDragging = false

local function UpdateSmoothnessSlider(input)
    local trackPos = SmoothnessTrack.AbsolutePosition.X
    local trackWidth = SmoothnessTrack.AbsoluteSize.X
    local mouseX = input.Position.X
    local pct = math.clamp((mouseX - trackPos) / trackWidth, 0, 1)
    local val = minSmooth + (maxSmooth - minSmooth) * pct
    _G.AimSmoothness = val
    SmoothnessFill.Size = UDim2.new(pct, 0, 1, 0)
    SmoothnessKnob.Position = UDim2.new(pct, 0, 0.5, 0)
    SmoothnessValueLabel.Text = string.format("%.2f", val)
end

SmoothnessTrack.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        smoothDragging = true
        UpdateSmoothnessSlider(input)
    end
end)

-- Forward-declare drag variables (used by consolidated handler below AND drag sections later)
local openBtnDragging, openBtnDragInput, openBtnDragStart, openBtnStartPos
local dragging, dragInput, dragStart, startPos

-- [[ CONSOLIDATED INPUT HANDLERS (performance: 1 connection instead of many) ]] --
UserInputService.InputChanged:Connect(function(input)
    local isMouse = input.UserInputType == Enum.UserInputType.MouseMovement
    local isTouch = input.UserInputType == Enum.UserInputType.Touch
    if not isMouse and not isTouch then return end

    -- Slider drags
    if fovDragging then UpdateFOVSlider(input) end
    if headshotDragging then UpdateHeadshotSlider(input) end
    if smoothDragging then UpdateSmoothnessSlider(input) end

    -- Window / button drags
    if input == openBtnDragInput and openBtnDragging then
        local delta = input.Position - openBtnDragStart
        OpenBtn.Position = UDim2.new(openBtnStartPos.X.Scale, openBtnStartPos.X.Offset + delta.X, openBtnStartPos.Y.Scale, openBtnStartPos.Y.Offset + delta.Y)
    end
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        if fovDragging or headshotDragging or smoothDragging then
            SaveConfig()
        end
        fovDragging = false
        headshotDragging = false
        smoothDragging = false
    end
end)

-- [[ TOGGLE BUTTONS SECTION ]] --
local ToggleGrid = Instance.new("Frame")
ToggleGrid.Name = "ToggleGrid"
ToggleGrid.BackgroundTransparency = 1
ToggleGrid.AutomaticSize = Enum.AutomaticSize.Y
ToggleGrid.Size = UDim2.new(1, 0, 0, 420)
ToggleGrid.Parent = Content

local GridLayout = Instance.new("UIGridLayout")
GridLayout.CellSize = UDim2.new(0, 155, 0, 40)
GridLayout.CellPadding = UDim2.new(0, 10, 0, 10)
GridLayout.Parent = ToggleGrid

local function CreateToggleButton(name, icon, isActive)
    local Button = Instance.new("TextButton")
    Button.Name = name
    Button.BackgroundColor3 = isActive and Theme.Accent or Theme.Surface
    Button.Size = UDim2.new(0, 155, 0, 40)
    Button.Font = Enum.Font.GothamBold
    Button.Text = ""
    Button.AutoButtonColor = false
    Button.Parent = ToggleGrid
    ApplyCorner(Button, 8)

    local stroke = ApplyStroke(Button, isActive and Theme.AccentGlow or Theme.Border, 1)

    local IconLabel = Instance.new("TextLabel")
    IconLabel.Name = "Icon"
    IconLabel.BackgroundTransparency = 1
    IconLabel.Position = UDim2.new(0, 12, 0, 0)
    IconLabel.Size = UDim2.new(0, 20, 1, 0)
    IconLabel.Font = Enum.Font.SourceSans
    IconLabel.Text = icon
    IconLabel.TextColor3 = isActive and Theme.Text or Theme.TextMuted
    IconLabel.TextSize = 16
    IconLabel.Parent = Button

    local Label = Instance.new("TextLabel")
    Label.Name = "Label"
    Label.BackgroundTransparency = 1
    Label.Position = UDim2.new(0, 35, 0, 0)
    Label.Size = UDim2.new(1, -70, 1, 0)
    Label.Font = Enum.Font.GothamMedium
    Label.Text = name
    Label.TextColor3 = isActive and Theme.Text or Theme.TextMuted
    Label.TextSize = 11
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Button

    local Status = Instance.new("Frame")
    Status.Name = "Status"
    Status.BackgroundColor3 = isActive and Theme.Success or Theme.Danger
    Status.Position = UDim2.new(1, -22, 0.5, -5)
    Status.Size = UDim2.new(0, 10, 0, 10)
    Status.Parent = Button
    ApplyCorner(Status, 5)

    Button.MouseEnter:Connect(function()
        CreateTween(Button, {BackgroundColor3 = isActive and Theme.AccentGlow or Theme.SurfaceHover}, 0.15):Play()
    end)

    Button.MouseLeave:Connect(function()
        CreateTween(Button, {BackgroundColor3 = isActive and Theme.Accent or Theme.Surface}, 0.15):Play()
    end)

    local function UpdateState(active)
        isActive = active
        CreateTween(Button, {BackgroundColor3 = active and Theme.Accent or Theme.Surface}, 0.2):Play()
        CreateTween(Status, {BackgroundColor3 = active and Theme.Success or Theme.Danger}, 0.2):Play()
        CreateTween(IconLabel, {TextColor3 = active and Theme.Text or Theme.TextMuted}, 0.2):Play()
        CreateTween(Label, {TextColor3 = active and Theme.Text or Theme.TextMuted}, 0.2):Play()
        stroke.Color = active and Theme.AccentGlow or Theme.Border
    end

    return Button, UpdateState
end

local NoclipBtn,   UpdateNoclip   = CreateToggleButton("Noclip",     "👻", false)
local FlyBtn,      UpdateFly      = CreateToggleButton("Fly",        "🚀", false)
local SpeedBtn,    UpdateSpeed    = CreateToggleButton("Speed",      "⚡", false)
local EspBtn,      UpdateEsp      = CreateToggleButton("ESP",        "👁", false)
local FreeCamBtn,  UpdateFreeCam  = CreateToggleButton("FreeCam",    "🎥", false)
local InfJumpBtn,  UpdateInfJump  = CreateToggleButton("Inf Jump",   "🦘", false)
local BrightBtn,   UpdateBright   = CreateToggleButton("FullBright", "☀️", false)
local SafeBtn,     UpdateSafe     = CreateToggleButton("Safe Mode",  "🛡",  true)
local ReachBtn,    UpdateReach    = CreateToggleButton("Reach",      "🗡", false)
local KillAuraBtn, UpdateKillAura = CreateToggleButton("Kill Aura",  "💀", false)
local AutoAimBtn,  UpdateAutoAim  = CreateToggleButton("Auto Aim [T]", "🎯", false)
local FOVBtn,      UpdateShowFOV  = CreateToggleButton("Show FOV",   "⭕", _G.ShowFOV)
local AimModeBtn,  UpdateAimMode  = CreateToggleButton("Smooth Aim",  "🔄", false)
local TeamCheckBtn, UpdateTeamCheck = CreateToggleButton("Team Check", "🤝", _G.TeamCheck)
local AutoShootBtn, UpdateAutoShoot = CreateToggleButton("Auto Shoot", "🔫", _G.AutoShoot)

-- [[ ACTION BUTTONS ]] --
local ActionSection = Instance.new("Frame")
ActionSection.Name = "ActionSection"
ActionSection.BackgroundTransparency = 1
ActionSection.Size = UDim2.new(1, 0, 0, 45)
ActionSection.Parent = Content

local function CreateActionButton(name, color, width)
    local Button = Instance.new("TextButton")
    Button.Name = name
    Button.BackgroundColor3 = color
    Button.Size = UDim2.new(0, width, 0, 40)
    Button.Font = Enum.Font.GothamBold
    Button.Text = name
    Button.TextColor3 = Theme.Text
    Button.TextSize = 11
    Button.AutoButtonColor = false
    Button.Parent = ActionSection
    ApplyCorner(Button, 8)

    Button.MouseEnter:Connect(function()
        CreateTween(Button, {BackgroundTransparency = 0.2}, 0.15):Play()
    end)
    Button.MouseLeave:Connect(function()
        CreateTween(Button, {BackgroundTransparency = 0}, 0.15):Play()
    end)

    return Button
end

local ActionLayout = Instance.new("UIListLayout")
ActionLayout.FillDirection = Enum.FillDirection.Horizontal
ActionLayout.Padding = UDim.new(0, 10)
ActionLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ActionLayout.Parent = ActionSection

local TpToolBtn       = CreateActionButton("🔧 TP Tool",  Color3.fromRGB(59, 130, 246), 100)
local TpPlayerBtn     = CreateActionButton("🎯 Teleport", Color3.fromRGB(168, 85, 247), 100)
local IgnorePlayerBtn = CreateActionButton("🚫 Ignore",   Color3.fromRGB(239, 68, 68), 100)

-- [[ PLAYER SELECTOR ]] --
local PlayerSection = Instance.new("Frame")
PlayerSection.Name = "PlayerSection"
PlayerSection.BackgroundColor3 = Theme.Surface
PlayerSection.Size = UDim2.new(1, 0, 0, 150)
PlayerSection.Parent = Content
ApplyCorner(PlayerSection, 10)
ApplyStroke(PlayerSection, Theme.Border, 1)

local PlayerHeader = Instance.new("Frame")
PlayerHeader.Name = "Header"
PlayerHeader.BackgroundColor3 = Theme.SurfaceHover
PlayerHeader.Size = UDim2.new(1, 0, 0, 32)
PlayerHeader.Parent = PlayerSection
ApplyCorner(PlayerHeader, 10)

local PlayerHeaderFix = Instance.new("Frame")
PlayerHeaderFix.BackgroundColor3 = Theme.SurfaceHover
PlayerHeaderFix.Position = UDim2.new(0, 0, 1, -10)
PlayerHeaderFix.Size = UDim2.new(1, 0, 0, 10)
PlayerHeaderFix.BorderSizePixel = 0
PlayerHeaderFix.Parent = PlayerHeader

local PlayerTitle = Instance.new("TextLabel")
PlayerTitle.BackgroundTransparency = 1
PlayerTitle.Position = UDim2.new(0, 12, 0, 0)
PlayerTitle.Size = UDim2.new(1, -24, 1, 0)
PlayerTitle.Font = Enum.Font.GothamBold
PlayerTitle.Text = "👥 Players Online"
PlayerTitle.TextColor3 = Theme.Text
PlayerTitle.TextSize = 11
PlayerTitle.TextXAlignment = Enum.TextXAlignment.Left
PlayerTitle.Parent = PlayerHeader

local PlayerCount = Instance.new("TextLabel")
PlayerCount.Name = "Count"
PlayerCount.BackgroundTransparency = 1
PlayerCount.Position = UDim2.new(1, -40, 0, 0)
PlayerCount.Size = UDim2.new(0, 30, 1, 0)
PlayerCount.Font = Enum.Font.GothamBold
PlayerCount.Text = "0"
PlayerCount.TextColor3 = Theme.Accent
PlayerCount.TextSize = 11
PlayerCount.Parent = PlayerHeader

local PlayerScroll = Instance.new("ScrollingFrame")
PlayerScroll.Name = "Scroll"
PlayerScroll.BackgroundTransparency = 1
PlayerScroll.Position = UDim2.new(0, 8, 0, 38)
PlayerScroll.Size = UDim2.new(1, -16, 1, -46)
PlayerScroll.ScrollBarThickness = 2
PlayerScroll.ScrollBarImageColor3 = Theme.Accent
PlayerScroll.ScrollBarImageTransparency = 0.5
PlayerScroll.Parent = PlayerSection

local PlayerListLayout = Instance.new("UIListLayout")
PlayerListLayout.Padding = UDim.new(0, 4)
PlayerListLayout.Parent = PlayerScroll

-- Open Button (when minimized)
local OpenBtn = Instance.new("TextButton")
OpenBtn.Name = "OpenBtn"
OpenBtn.BackgroundColor3 = Theme.Accent
OpenBtn.Position = UDim2.new(0, 20, 0, 20)
OpenBtn.Size = UDim2.new(0, 50, 0, 50)
OpenBtn.Font = Enum.Font.GothamBlack
OpenBtn.Text = "Z"
OpenBtn.TextColor3 = Theme.Text
OpenBtn.TextSize = 20
OpenBtn.Visible = false
OpenBtn.ZIndex = 100
OpenBtn.Parent = ScreenGui
ApplyCorner(OpenBtn, 25)
ApplyStroke(OpenBtn, Theme.AccentGlow, 2)

-- Make OpenBtn draggable (variables forward-declared above)

OpenBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        openBtnDragging = true
        openBtnDragStart = input.Position
        openBtnStartPos = OpenBtn.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                openBtnDragging = false
            end
        end)
    end
end)

OpenBtn.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        openBtnDragInput = input
    end
end)


-- [[ DRAGGING ]] -- (variables forward-declared above)

Header.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Main.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Header.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)


-- [[ PLAYER LIST REFRESH ]] --
local function RefreshPlayers()
    for _, v in pairs(PlayerScroll:GetChildren()) do
        if v:IsA("Frame") or v:IsA("TextButton") then v:Destroy() end
    end

    local count = 0
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            count = count + 1

            local isIgnored = _G.IgnoredPlayers[p.Name] == true
            local isSelected = (_G.SelectedPlayer == p.Name)

            local PlayerRow = Instance.new("Frame")
            PlayerRow.Name = p.Name
            PlayerRow.BackgroundTransparency = 1
            PlayerRow.Size = UDim2.new(1, 0, 0, 28)
            PlayerRow.Parent = PlayerScroll

            local PlayerBtn = Instance.new("TextButton")
            PlayerBtn.Name = "SelectBtn"
            PlayerBtn.BackgroundColor3 = isSelected and Theme.Accent or Theme.SurfaceHover
            PlayerBtn.Size = UDim2.new(1, -70, 1, 0)
            PlayerBtn.Font = Enum.Font.GothamMedium
            PlayerBtn.Text = "  " .. p.DisplayName
            PlayerBtn.TextColor3 = isSelected and Theme.Text or Theme.TextMuted
            PlayerBtn.TextSize = 11
            PlayerBtn.TextXAlignment = Enum.TextXAlignment.Left
            PlayerBtn.AutoButtonColor = false
            PlayerBtn.Parent = PlayerRow
            ApplyCorner(PlayerBtn, 6)

            local IgnoreBtn = Instance.new("TextButton")
            IgnoreBtn.Name = "IgnoreBtn"
            IgnoreBtn.BackgroundColor3 = isIgnored and Theme.Danger or Theme.SurfaceHover
            IgnoreBtn.Position = UDim2.new(1, -65, 0, 0)
            IgnoreBtn.Size = UDim2.new(0, 65, 1, 0)
            IgnoreBtn.Font = Enum.Font.GothamBold
            IgnoreBtn.Text = isIgnored and "Ignored" or "Ignore"
            IgnoreBtn.TextColor3 = isIgnored and Theme.Text or Theme.TextMuted
            IgnoreBtn.TextSize = 10
            IgnoreBtn.AutoButtonColor = false
            IgnoreBtn.Parent = PlayerRow
            ApplyCorner(IgnoreBtn, 6)

            PlayerBtn.MouseEnter:Connect(function()
                if _G.SelectedPlayer ~= p.Name then
                    CreateTween(PlayerBtn, {BackgroundColor3 = Theme.Border}, 0.1):Play()
                end
            end)

            PlayerBtn.MouseLeave:Connect(function()
                if _G.SelectedPlayer ~= p.Name then
                    CreateTween(PlayerBtn, {BackgroundColor3 = Theme.SurfaceHover}, 0.1):Play()
                end
            end)

            PlayerBtn.MouseButton1Click:Connect(function()
                _G.SelectedPlayer = p.Name
                RefreshPlayers()
            end)

            IgnoreBtn.MouseButton1Click:Connect(function()
                local newState = not (_G.IgnoredPlayers[p.Name] or false)
                _G.IgnoredPlayers[p.Name] = newState
                _G.IgnoredPlayers[p.DisplayName] = newState
                _G.IgnoredPlayers[tostring(p.UserId)] = newState
                RefreshPlayers()
            end)
        end
    end

    PlayerCount.Text = tostring(count)
    PlayerScroll.CanvasSize = UDim2.new(0, 0, 0, count * 32)
end

RefreshPlayers()
Players.PlayerAdded:Connect(RefreshPlayers)
Players.PlayerRemoving:Connect(RefreshPlayers)

-- [[ SESSION TIMER ]] --
task.spawn(function()
    local startTime = tick()
    while task.wait(1) do
        local elapsed = tick() - startTime
        local h = math.floor(elapsed / 3600)
        local m = math.floor((elapsed % 3600) / 60)
        local s = math.floor(elapsed % 60)
        SessionLabel.Text = string.format("%02d:%02d:%02d", h, m, s)
    end
end)

-- [[ BUTTON LOGIC ]] --
CloseBtn.MouseButton1Click:Connect(function()
    if FOVCircle then pcall(function() FOVCircle:Remove() end) end
    CreateTween(Main, {Size = UDim2.new(0, 360, 0, 0), BackgroundTransparency = 1}, 0.3):Play()
    task.wait(0.3)
    ScreenGui:Destroy()
end)

-- Minimize Button Logic
local isMinimized = false
local savedMainPosition = nil

MinimizeBtn.MouseButton1Click:Connect(function()
    if isMinimized then return end
    isMinimized = true
    savedMainPosition = Main.Position

    local tweenOut = CreateTween(Main, {
        Size = UDim2.new(0, 360, 0, 50),
        BackgroundTransparency = 0.5
    }, 0.25)
    tweenOut:Play()
    tweenOut.Completed:Wait()

    Main.Visible = false
    OpenBtn.Visible = true

    OpenBtn.Size = UDim2.new(0, 0, 0, 0)
    OpenBtn.BackgroundTransparency = 1
    local tweenIn = CreateTween(OpenBtn, {
        Size = UDim2.new(0, 50, 0, 50),
        BackgroundTransparency = 0
    }, 0.2, Enum.EasingStyle.Back)
    tweenIn:Play()
end)

-- Open Button Logic
OpenBtn.MouseButton1Click:Connect(function()
    if not isMinimized then return end

    local tweenOut = CreateTween(OpenBtn, {
        Size = UDim2.new(0, 0, 0, 0),
        BackgroundTransparency = 1
    }, 0.15)
    tweenOut:Play()
    tweenOut.Completed:Wait()

    OpenBtn.Visible = false
    Main.Visible = true

    Main.Size = UDim2.new(0, 360, 0, 50)
    Main.BackgroundTransparency = 0.5
    if savedMainPosition then
        Main.Position = savedMainPosition
    end

    local tweenIn = CreateTween(Main, {
        Size = UDim2.new(0, 360, 0, 480),
        BackgroundTransparency = 0
    }, 0.3, Enum.EasingStyle.Back)
    tweenIn:Play()

    isMinimized = false
end)

-- Hover effects for OpenBtn
OpenBtn.MouseEnter:Connect(function()
    CreateTween(OpenBtn, {Size = UDim2.new(0, 55, 0, 55)}, 0.1):Play()
end)

OpenBtn.MouseLeave:Connect(function()
    CreateTween(OpenBtn, {Size = UDim2.new(0, 50, 0, 50)}, 0.1):Play()
end)

-- Input handlers
SpeedInput.FocusLost:Connect(function()
    local val = tonumber(SpeedInput.Text:match("%d+"))
    if val then _G.MovementSpeed = val end
    SpeedInput.Text = tostring(_G.MovementSpeed)
end)

TimeInput.FocusLost:Connect(function()
    local val = tonumber(TimeInput.Text:match("%d+%.?%d*"))
    if val then _G.TimeScale = val end
    TimeInput.Text = tostring(_G.TimeScale) .. "x"
end)

ReachInput.FocusLost:Connect(function()
    local val = tonumber(ReachInput.Text:match("%d+%.?%d*"))
    if val then
        _G.ReachDistance = math.clamp(val, 1, 100)
    end
    ReachInput.Text = tostring(_G.ReachDistance)
end)

KillAuraRangeInput.FocusLost:Connect(function()
    local val = tonumber(KillAuraRangeInput.Text:match("%d+%.?%d*"))
    if val then
        _G.KillAuraRange = math.clamp(val, 1, 200)
    end
    KillAuraRangeInput.Text = tostring(_G.KillAuraRange)
end)

ESPRangeInput.FocusLost:Connect(function()
    local val = tonumber(ESPRangeInput.Text:match("%d+%.?%d*"))
    if val then
        _G.ESPRange = math.clamp(val, 10, 100000)
    end
    ESPRangeInput.Text = tostring(_G.ESPRange)
end)

AimFOVInput.FocusLost:Connect(function()
    local val = tonumber(AimFOVInput.Text:match("%d+"))
    if val then
        _G.AimFOV = math.clamp(val, minFOV, maxFOV)
        local pct = math.clamp((_G.AimFOV - minFOV) / (maxFOV - minFOV), 0, 1)
        AimFOVFill.Size = UDim2.new(pct, 0, 1, 0)
        AimFOVKnob.Position = UDim2.new(pct, 0, 0.5, 0)
        SaveConfig()
    end
    AimFOVInput.Text = tostring(_G.AimFOV) .. " px"
end)

-- [[ CORE FUNCTIONS ]] --
local function RestoreLighting()
    Lighting.Brightness = LightCache.Brightness
    Lighting.ClockTime = LightCache.ClockTime
    Lighting.FogEnd = LightCache.FogEnd
    Lighting.GlobalShadows = LightCache.GlobalShadows
    Lighting.OutdoorAmbient = LightCache.OutdoorAmbient
end

local FreeCamPart, freecamConnection
local function ToggleFreeCam()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")

    if _G.FreeCam then
        if hrp then hrp.Anchored = true end
        if not FreeCamPart then
            FreeCamPart = Instance.new("Part")
            FreeCamPart.Anchored = true
            FreeCamPart.CanCollide = false
            FreeCamPart.Transparency = 1
            FreeCamPart.CFrame = Camera.CFrame
            FreeCamPart.Parent = workspace
        end
        Camera.CameraSubject = FreeCamPart

        freecamConnection = RunService.RenderStepped:Connect(function(dt)
            local dir = Vector3.new()
            local hum = char and char:FindFirstChild("Humanoid")
            if hum and hum.MoveDirection.Magnitude > 0 then
                dir = hum.MoveDirection
            else
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.E) then dir = dir + Vector3.new(0, 1, 0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.Q) then dir = dir - Vector3.new(0, 1, 0) end
            end

            local camSpeed = _G.SpeedEnabled and _G.MovementSpeed or 50
            if dir.Magnitude > 0 then
                FreeCamPart.CFrame = FreeCamPart.CFrame + (dir.Unit * (camSpeed * dt))
            end
        end)
    else
        if hrp then hrp.Anchored = false end
        if freecamConnection then freecamConnection:Disconnect() end
        if FreeCamPart then FreeCamPart:Destroy(); FreeCamPart = nil end
        if char and char:FindFirstChild("Humanoid") then
            Camera.CameraSubject = char.Humanoid
        end
    end
end

local function StopFly()
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChild("Humanoid")
        if hrp then
            local bv = hrp:FindFirstChild("VelocityGrip")
            local bg = hrp:FindFirstChild("GyroGrip")
            if bv then bv:Destroy() end
            if bg then bg:Destroy() end
        end
        if hum then
            hum.PlatformStand = false
            hum:ChangeState(Enum.HumanoidStateType.Running)
        end
    end
end

-- [[ REACH SYSTEM ]] --
local ReachPart = nil
local OriginalSizes = {}

local function CreateReachPart()
    local char = LocalPlayer.Character
    if not char then return end

    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if ReachPart then ReachPart:Destroy() end

    ReachPart = Instance.new("Part")
    ReachPart.Name = "ReachPart"
    ReachPart.Size = Vector3.new(_G.ReachDistance, _G.ReachDistance, _G.ReachDistance)
    ReachPart.Transparency = 1
    ReachPart.CanCollide = false
    ReachPart.Anchored = false
    ReachPart.Massless = true
    ReachPart.Parent = char

    local weld = Instance.new("Weld")
    weld.Part0 = hrp
    weld.Part1 = ReachPart
    weld.C0 = CFrame.new(0, 0, -_G.ReachDistance / 2)
    weld.Parent = ReachPart

    ReachPart.Touched:Connect(function(hit)
        if not _G.ReachEnabled then return end

        local humanoid = hit.Parent:FindFirstChild("Humanoid")
        if humanoid and hit.Parent ~= char then
            firetouchinterest(ReachPart, hit, 0)
            task.wait()
            firetouchinterest(ReachPart, hit, 1)
        end
    end)
end

local function UpdateReachSize()
    if ReachPart then
        ReachPart.Size = Vector3.new(_G.ReachDistance, _G.ReachDistance, _G.ReachDistance)
        local weld = ReachPart:FindFirstChild("Weld")
        if weld then
            weld.C0 = CFrame.new(0, 0, -_G.ReachDistance / 2)
        end
    end
end

local function DestroyReachPart()
    if ReachPart then
        ReachPart:Destroy()
        ReachPart = nil
    end
end

local function ExpandToolHitbox()
    local char = LocalPlayer.Character
    if not char then return end

    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") then
            local handle = tool:FindFirstChild("Handle")
            if handle and handle:IsA("BasePart") then
                if not OriginalSizes[handle] then
                    OriginalSizes[handle] = handle.Size
                end
                handle.Size = Vector3.new(_G.ReachDistance, _G.ReachDistance, _G.ReachDistance)
                handle.Massless = true
                handle.Transparency = 1
            end
        end
    end
end

local function RestoreToolHitbox()
    for handle, originalSize in pairs(OriginalSizes) do
        if handle and handle.Parent then
            handle.Size = originalSize
        end
    end
    OriginalSizes = {}
end

-- [[ KILL AURA SYSTEM ]] --
-- Tracks last-hit time per NPC to throttle damage ticks
local KillAuraLastHit = {}
-- How many times per second kill aura deals damage
local KILL_AURA_RATE = 0.1 -- seconds between damage applications per NPC

--[[
    IsPlayerCharacter: returns true if a model belongs to a real player.
    Uses GetPlayerFromCharacter (O(1)) instead of looping all players.
]]
local function IsPlayerCharacter(model)
    return Players:GetPlayerFromCharacter(model) ~= nil
end

--[[
    GetNPCsInRange: scans workspace for models with a Humanoid and a
    HumanoidRootPart that are (a) not a player character, (b) alive,
    and (c) within _G.KillAuraRange studs of our HRP.
    OPTIMIZED: caches scan results for 0.5 seconds to avoid
    calling workspace:GetDescendants() every frame.
]]
local _npcCache = {}
local _npcCacheTime = 0
local NPC_CACHE_DURATION = 0.5 -- rescan every 0.5 seconds

local function GetNPCsInRange(hrp)
    local results = {}
    local range = _G.KillAuraRange
    local now = tick()

    -- Refresh NPC list only every NPC_CACHE_DURATION seconds
    if (now - _npcCacheTime) >= NPC_CACHE_DURATION then
        _npcCacheTime = now
        _npcCache = {}

        local function ScanFolder(parent, depth)
            if (depth or 0) > 3 then return end -- max folder depth 3
            for _, child in pairs(parent:GetChildren()) do
                if child:IsA("Model") and not IsPlayerCharacter(child) then
                    local hum = child:FindFirstChildOfClass("Humanoid")
                    if hum and hum.Health > 0 then
                        local root = child:FindFirstChild("HumanoidRootPart")
                            or child:FindFirstChild("Torso")
                            or child:FindFirstChildWhichIsA("BasePart")
                        if root then
                            table.insert(_npcCache, {humanoid = hum, root = root})
                        end
                    end
                elseif child:IsA("Folder") or child:IsA("Model") or child.Name == "NPCs" or child.Name == "Enemies" or child.Name == "Monsters" or child.Name == "Bots" then
                    ScanFolder(child, (depth or 0) + 1)
                end
            end
        end

        ScanFolder(workspace, 0)
    end

    -- Filter cached NPCs by range
    for _, entry in pairs(_npcCache) do
        if entry.humanoid and entry.humanoid.Parent and entry.humanoid.Health > 0 then
            local dist = (entry.root.Position - hrp.Position).Magnitude
            if dist <= range then
                table.insert(results, entry)
            end
        end
    end

    return results
end

--[[
    RunKillAura: called every Stepped tick.
    Applies damage to every NPC in range, rate-limited per NPC so we
    don't spam TakeDamage every frame (which can cause issues).
    Damage method priority:
      1. TakeDamage (standard, works on most games)
      2. Fires the tool's activation if the player has a damage tool equipped
         (fallback for games that block TakeDamage via HealthChanged guards)
]]
local function RunKillAura()
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local now = tick()
    local npcs = GetNPCsInRange(hrp)

    for _, entry in pairs(npcs) do
        local hum = entry.humanoid
        local key = tostring(hum)

        -- Rate-limit: only damage each NPC every KILL_AURA_RATE seconds
        if not KillAuraLastHit[key] or (now - KillAuraLastHit[key]) >= KILL_AURA_RATE then
            KillAuraLastHit[key] = now

            -- Primary method: TakeDamage
            local ok, err = pcall(function()
                hum:TakeDamage(math.huge)
            end)

            -- Fallback: if TakeDamage is blocked, try setting Health directly
            if not ok or hum.Health > 0 then
                pcall(function()
                    hum.Health = 0
                end)
            end

            -- Secondary fallback: simulate tool hit via firetouchinterest on HRP
            if hum.Health > 0 then
                pcall(function()
                    firetouchinterest(hrp, entry.root, 0)
                    firetouchinterest(hrp, entry.root, 1)
                end)
            end
        end
    end

    -- Clean up stale keys so the table doesn't grow forever
    for key, lastTime in pairs(KillAuraLastHit) do
        if (now - lastTime) > 10 then
            KillAuraLastHit[key] = nil
        end
    end
end

-- [[ AUTO AIM & FOV CIRCLE SYSTEM ]] --
if _G.ZHubFOVCircle then
    pcall(function() _G.ZHubFOVCircle:Remove() end)
    pcall(function() _G.ZHubFOVCircle:Destroy() end)
    _G.ZHubFOVCircle = nil
end

-- Purge any orphaned FOVCircleGui frames safely
if parentContainer then
    for _, child in pairs(parentContainer:GetChildren()) do
        if child.Name == "FOVCircleGui" or child.Name == "ZHubFOVCircle" then
            pcall(function() child:Destroy() end)
        end
    end
end

local FOVCircle = nil
pcall(function()
    if Drawing and typeof(Drawing.new) == "function" then
        FOVCircle = Drawing.new("Circle")
        FOVCircle.Thickness = 1.5
        FOVCircle.Color = Theme.Accent
        FOVCircle.Filled = false
        FOVCircle.Transparency = 0.7
        FOVCircle.Radius = _G.AimFOV
        FOVCircle.Visible = false
        _G.ZHubFOVCircle = FOVCircle
    end
end)

local GuiFOVCircle = nil
if not FOVCircle then
    GuiFOVCircle = Instance.new("Frame")
    GuiFOVCircle.Name = "FOVCircleGui"
    GuiFOVCircle.BackgroundTransparency = 1
    GuiFOVCircle.AnchorPoint = Vector2.new(0.5, 0.5)
    GuiFOVCircle.Visible = false
    GuiFOVCircle.Parent = ScreenGui
    ApplyCorner(GuiFOVCircle, 999)
    ApplyStroke(GuiFOVCircle, Theme.Accent, 1.5, 0.3)
end

local function GetCrosshairPosition()
    return Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
end

local function UpdateFOVCircle()
    local shouldShow = _G.ShowFOV and (_G.AutoAim or _G.AutoShoot or _G.ShowFOV)

    if FOVCircle then
        FOVCircle.Visible = shouldShow
    elseif GuiFOVCircle then
        GuiFOVCircle.Visible = shouldShow
    end

    if not shouldShow then return end

    -- Dot mode: small 5px circle when Auto Aim is disabled & Auto Shoot is enabled
    local isDotMode = (not _G.AutoAim) and _G.AutoShoot
    local fovRadius = isDotMode and 5 or _G.AimFOV

    local crosshairPos = GetCrosshairPosition()
    if FOVCircle then
        FOVCircle.Radius = fovRadius
        FOVCircle.Position = crosshairPos
    elseif GuiFOVCircle then
        GuiFOVCircle.Size = UDim2.new(0, fovRadius * 2, 0, fovRadius * 2)
        GuiFOVCircle.Position = UDim2.new(0, crosshairPos.X, 0, crosshairPos.Y)
    end
end

-- [[ WALL CHECK ]] --
local AimRayParams = RaycastParams.new()
AimRayParams.FilterType = Enum.RaycastFilterType.Exclude
AimRayParams.IgnoreWater = true

local function IsPartVisible(targetPart)
    if not _G.AimWallCheck then return true end
    local myChar = LocalPlayer.Character
    if not myChar or not targetPart then return false end

    local origin = Camera.CFrame.Position
    local direction = targetPart.Position - origin
    if direction.Magnitude < 0.5 then return true end

    -- Build exclude list once
    AimRayParams.FilterDescendantsInstances = {myChar, targetPart.Parent}

    local hit = workspace:Raycast(origin, direction, AimRayParams)
    if not hit then return true end

    local inst = hit.Instance
    if not inst then return true end

    -- Allow shots through: own character parts, non-collidable, transparent, accessories
    if inst:IsDescendantOf(targetPart.Parent) then return true end
    if inst.CanCollide == false then return true end
    if inst.Transparency >= 0.5 then return true end
    if inst:IsA("Accessory") or inst:FindFirstAncestorWhichIsA("Accessory") then return true end

    return false
end

-- [[ TARGET SELECTION ]] --
local CurrentLockedTarget = nil  -- The character Model we're locked onto
local CurrentAimIsHead = false   -- Whether this engagement aims at head height

-- [[ TEAMMATE / HARMABILITY CHECK ]] --
local function IsTeammate(player)
    if not _G.TeamCheck then return false end
    if not player or player == LocalPlayer then return false end

    -- Team check: same team = teammate
    if LocalPlayer.Team and player.Team and LocalPlayer.Team == player.Team then
        -- Both neutral means no team system, don't skip
        if not (LocalPlayer.Neutral and player.Neutral) then
            return true
        end
    end

    -- ForceField check: player has spawn protection
    local char = player.Character
    if char and char:FindFirstChildOfClass("ForceField") then
        return true
    end

    return false
end

local function FindBestTarget()
    local crosshairPos = GetCrosshairPosition()
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")

    -- Sticky lock: keep tracking same character if still valid (prevents target-hopping jitter)
    if CurrentLockedTarget and CurrentLockedTarget.Parent then
        local hum = CurrentLockedTarget:FindFirstChildOfClass("Humanoid")
        local player = Players:GetPlayerFromCharacter(CurrentLockedTarget)
        if player then
            local ign = _G.IgnoredPlayers[player.Name] or _G.IgnoredPlayers[player.DisplayName] or _G.IgnoredPlayers[tostring(player.UserId)]
            if not ign and not IsTeammate(player) and hum and hum.Health > 0 then
                local hrp = CurrentLockedTarget:FindFirstChild("HumanoidRootPart") or CurrentLockedTarget:FindFirstChild("Torso")
                if hrp then
                    if myHRP and (hrp.Position - myHRP.Position).Magnitude <= _G.ESPRange then
                        local sp, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                        if onScreen then
                            local d = (Vector2.new(sp.X, sp.Y) - crosshairPos).Magnitude
                            if d <= _G.AimFOV * 1.5 then
                                local head = CurrentLockedTarget:FindFirstChild("Head")
                                if IsPartVisible(hrp) or (head and IsPartVisible(head)) then
                                    return CurrentLockedTarget
                                end
                            end
                        end
                    end
                end
            end
        end
    end

    -- Find new closest target (FAST FILTER FIRST: screen position & FOV distance -> SLOW CHECK: Raycast)
    local isDotMode = (not _G.AutoAim) and _G.AutoShoot
    local effectiveFOV = isDotMode and 12 or _G.AimFOV

    local bestChar = nil
    local bestDist = effectiveFOV

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local char = p.Character
            if char then
                local hrp = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
                if hrp then
                    local hum = char:FindFirstChildOfClass("Humanoid")
                    if hum and hum.Health > 0 then
                        local dist3D = myHRP and (hrp.Position - myHRP.Position).Magnitude or 0
                        if dist3D <= _G.ESPRange then
                            -- Fast Check 1: Is player on screen?
                            local sp, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                            if onScreen then
                                -- Fast Check 2: Is 2D distance to crosshair closer than bestDist?
                                local d = (Vector2.new(sp.X, sp.Y) - crosshairPos).Magnitude
                                if d < bestDist then
                                    -- Fast Check 3: Is player ignored or teammate?
                                    local ign = _G.IgnoredPlayers[p.Name] or _G.IgnoredPlayers[p.DisplayName] or _G.IgnoredPlayers[tostring(p.UserId)]
                                    if not ign and not IsTeammate(p) then
                                        -- Slow Check 4: Wall check raycast ONLY for the 1 candidate inside FOV!
                                        local head = char:FindFirstChild("Head")
                                        if IsPartVisible(hrp) or (head and IsPartVisible(head)) then
                                            bestDist = d
                                            bestChar = char
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end

    -- New target acquired — roll headshot chance once per engagement
    if bestChar ~= CurrentLockedTarget then
        CurrentLockedTarget = bestChar
        CurrentAimIsHead = bestChar and (math.random() <= (_G.HeadshotChance or 0.40))
    end

    return bestChar
end

-- [[ CAMERA TRACKING ]] --
local function RunAutoAim(dt)
    if not _G.AutoAim then
        CurrentLockedTarget = nil
        return
    end

    local targetChar = FindBestTarget()
    if not targetChar then
        CurrentLockedTarget = nil
        return
    end

    local hrp = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso")
    if not hrp then return end

    -- Stable aim point: HumanoidRootPart position + fixed Y offset (no animation jitter)
    local yOffset = CurrentAimIsHead and 1.5 or 0
    local aimPoint = hrp.Position + Vector3.new(0, yOffset, 0)

    -- Gentle velocity lead (only horizontal, avoids vertical overshoot from jumping)
    local vel = Vector3.new()
    pcall(function()
        local v = hrp.AssemblyLinearVelocity or hrp.Velocity
        if v then vel = Vector3.new(v.X, 0, v.Z) end
    end)
    aimPoint = aimPoint + vel * 0.02

    if _G.SmoothAim then
        -- Smooth Aim: lerp the camera towards the target using AimSmoothness
        local targetCFrame = CFrame.new(Camera.CFrame.Position, aimPoint)
        local alpha = math.clamp(_G.AimSmoothness * (dt * 60), 0, 1)
        Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, alpha)
    else
        -- Direct Aim: snap camera directly to target every frame (instant lock)
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, aimPoint)
    end
end

-- [[ AUTO SHOOT SYSTEM ]] --
local _lastShootTime = 0
local AUTO_SHOOT_RATE = 0.1 -- 10 shots per second max

local function RunAutoShoot()
    if not _G.AutoShoot then return end

    local targetChar = CurrentLockedTarget
    if not targetChar or not targetChar.Parent then
        targetChar = FindBestTarget()
    end
    if not targetChar or not targetChar.Parent then return end

    local now = tick()
    if (now - _lastShootTime) < AUTO_SHOOT_RATE then return end
    _lastShootTime = now

    local char = LocalPlayer.Character
    if not char then return end

    -- Primary: Tool Activate
    local tool = char:FindFirstChildOfClass("Tool")
    if tool then
        pcall(function() tool:Activate() end)
    end

    -- Secondary: VirtualInputManager / mouse click simulation
    pcall(function()
        if mouse1click then
            mouse1click()
        elseif mouse1press then
            mouse1press()
            task.delay(0.02, function() pcall(mouse1release) end)
        else
            local vim = game:GetService("VirtualInputManager")
            local vp = Camera.ViewportSize
            vim:SendMouseButtonEvent(vp.X / 2, vp.Y / 2, 0, true, game, 0)
            task.delay(0.02, function()
                vim:SendMouseButtonEvent(vp.X / 2, vp.Y / 2, 0, false, game, 0)
            end)
        end
    end)
end

-- Toggle button connections
NoclipBtn.MouseButton1Click:Connect(function()
    _G.Noclip = not _G.Noclip
    UpdateNoclip(_G.Noclip)
end)

FlyBtn.MouseButton1Click:Connect(function()
    _G.Fly = not _G.Fly
    UpdateFly(_G.Fly)
    if not _G.Fly then StopFly() end
end)

SpeedBtn.MouseButton1Click:Connect(function()
    _G.SpeedEnabled = not _G.SpeedEnabled
    UpdateSpeed(_G.SpeedEnabled)
end)

-- [[ ZERO-ALLOCATION ESP SYSTEM ]] --
local function GetOrCreateESP(player)
    if player == LocalPlayer then return nil, nil end
    local char = player.Character
    if not char or not char:FindFirstChild("Head") then return nil, nil end

    local high = char:FindFirstChild("ZESP")
    if not high then
        high = Instance.new("Highlight")
        high.Name = "ZESP"
        high.FillColor = Theme.Accent
        high.FillTransparency = 0.5
        high.OutlineColor = Theme.AccentGlow
        high.OutlineTransparency = 0
        high.Enabled = false
        high.Parent = char
    end

    local head = char:FindFirstChild("Head")
    local tag = char:FindFirstChild("ZTag") or (head and head:FindFirstChild("ZTag"))
    if not tag then
        tag = Instance.new("BillboardGui")
        tag.Name = "ZTag"
        tag.Size = UDim2.new(0, 100, 0, 40)
        tag.AlwaysOnTop = true
        tag.ExtentsOffset = Vector3.new(0, 2.5, 0)
        tag.MaxDistance = _G.ESPRange or 1000
        tag.Enabled = false
        if head then tag.Adornee = head end
        tag.Parent = char

        local tl = Instance.new("TextLabel")
        tl.Size = UDim2.new(1, 0, 1, 0)
        tl.BackgroundTransparency = 1
        tl.TextColor3 = Theme.Text
        tl.TextStrokeColor3 = Theme.Background
        tl.TextStrokeTransparency = 0.5
        tl.Font = Enum.Font.GothamBold
        tl.TextSize = 13
        tl.Text = player.DisplayName
        tl.Parent = tag
    end

    return high, tag
end

local function SetESPState(player, visible)
    if player == LocalPlayer then return end
    local char = player.Character
    if not char then return end

    local head = char:FindFirstChild("Head")
    local high = char:FindFirstChild("ZESP")
    local tag = char:FindFirstChild("ZTag") or (head and head:FindFirstChild("ZTag"))

    if visible then
        if not high or not tag then
            high, tag = GetOrCreateESP(player)
        end
        if high then high.Enabled = true end
        if tag then
            tag.MaxDistance = _G.ESPRange or 1000
            tag.Enabled = true
        end
    else
        if high then high.Enabled = false end
        if tag then tag.Enabled = false end
    end
end

-- ESP_RANGE is now controlled by _G.ESPRange (default 1000 studs)

local ESPRayParams = RaycastParams.new()
ESPRayParams.FilterType = Enum.RaycastFilterType.Exclude
ESPRayParams.IgnoreWater = true

local function IsPlayerOccluded(targetChar)
    local myChar = LocalPlayer.Character
    if not myChar or not targetChar then return false end

    local hrp = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso") or targetChar:FindFirstChild("Head")
    if not hrp then return false end

    local origin = Camera.CFrame.Position
    local direction = hrp.Position - origin
    local dist = direction.Magnitude
    if dist < 0.5 or dist > _G.ESPRange then return false end

    -- Fast check: don't raycast for players off-screen
    local _, onScreen = Camera:WorldToViewportPoint(hrp.Position)
    if not onScreen then return false end

    ESPRayParams.FilterDescendantsInstances = {myChar, targetChar}

    local hit = workspace:Raycast(origin, direction, ESPRayParams)
    if not hit then return false end

    local inst = hit.Instance
    if not inst then return false end

    if inst:IsDescendantOf(targetChar) or inst:IsDescendantOf(myChar) then return false end
    if inst.CanCollide == false then return false end
    if inst.Transparency >= 0.5 then return false end
    if inst:IsA("Accessory") or inst:FindFirstAncestorWhichIsA("Accessory") then return false end

    return true
end

local function RefreshAllESP()
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            if _G.EspEnabled and myHRP then
                local theirChar = p.Character
                if theirChar then
                    local theirHRP = theirChar:FindFirstChild("HumanoidRootPart") or theirChar:FindFirstChild("Torso")
                    if theirHRP and (theirHRP.Position - myHRP.Position).Magnitude <= _G.ESPRange and IsPlayerOccluded(theirChar) then
                        SetESPState(p, true)
                    else
                        SetESPState(p, false)
                    end
                else
                    SetESPState(p, false)
                end
            else
                SetESPState(p, false)
            end
        end
    end
end

-- Auto-apply ESP to new characters
local function HookPlayerESP(player)
    if player == LocalPlayer then return end
    player.CharacterAdded:Connect(function()
        task.wait(0.5)
        if _G.EspEnabled then
            RefreshAllESP()
        end
    end)
end

for _, p in pairs(Players:GetPlayers()) do
    HookPlayerESP(p)
end
Players.PlayerAdded:Connect(HookPlayerESP)

EspBtn.MouseButton1Click:Connect(function()
    _G.EspEnabled = not _G.EspEnabled
    UpdateEsp(_G.EspEnabled)
    RefreshAllESP()
end)

FreeCamBtn.MouseButton1Click:Connect(function()
    _G.FreeCam = not _G.FreeCam
    UpdateFreeCam(_G.FreeCam)
    ToggleFreeCam()
end)

InfJumpBtn.MouseButton1Click:Connect(function()
    _G.InfJump = not _G.InfJump
    UpdateInfJump(_G.InfJump)
end)

BrightBtn.MouseButton1Click:Connect(function()
    _G.FullBright = not _G.FullBright
    UpdateBright(_G.FullBright)
    if not _G.FullBright then RestoreLighting() end
end)

SafeBtn.MouseButton1Click:Connect(function()
    _G.SafeMode = not _G.SafeMode
    UpdateSafe(_G.SafeMode)
end)

ReachBtn.MouseButton1Click:Connect(function()
    _G.ReachEnabled = not _G.ReachEnabled
    UpdateReach(_G.ReachEnabled)
    if _G.ReachEnabled then
        CreateReachPart()
    else
        DestroyReachPart()
        RestoreToolHitbox()
    end
end)

KillAuraBtn.MouseButton1Click:Connect(function()
    _G.KillAura = not _G.KillAura
    UpdateKillAura(_G.KillAura)
    -- Clear stale hit cache when toggling off
    if not _G.KillAura then
        KillAuraLastHit = {}
    end
end)

local function ToggleAutoAim(forcedState)
    if forcedState ~= nil then
        _G.AutoAim = forcedState
    else
        _G.AutoAim = not _G.AutoAim
    end
    UpdateAutoAim(_G.AutoAim)
    SaveConfig()
end

AutoAimBtn.MouseButton1Click:Connect(function()
    ToggleAutoAim()
end)

FOVBtn.MouseButton1Click:Connect(function()
    _G.ShowFOV = not _G.ShowFOV
    UpdateShowFOV(_G.ShowFOV)
    SaveConfig()
end)

AimModeBtn.MouseButton1Click:Connect(function()
    _G.SmoothAim = not _G.SmoothAim
    UpdateAimMode(_G.SmoothAim)
end)

TeamCheckBtn.MouseButton1Click:Connect(function()
    _G.TeamCheck = not _G.TeamCheck
    UpdateTeamCheck(_G.TeamCheck)
    SaveConfig()
end)

AutoShootBtn.MouseButton1Click:Connect(function()
    _G.AutoShoot = not _G.AutoShoot
    UpdateAutoShoot(_G.AutoShoot)
    SaveConfig()
end)

-- TP Tool
local tpClaimed = false
TpToolBtn.MouseButton1Click:Connect(function()
    if tpClaimed then return end
    tpClaimed = true
    TpToolBtn.Text = "✓ Claimed"
    TpToolBtn.BackgroundColor3 = Theme.Success

    local tool = Instance.new("Tool")
    tool.Name = "TP Tool"
    tool.RequiresHandle = false
    tool.Parent = LocalPlayer.Backpack

    tool.Activated:Connect(function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local dist = (char.HumanoidRootPart.Position - Mouse.Hit.p).Magnitude
            local speed = _G.SafeMode and 100 or 500
            TweenService:Create(
                char.HumanoidRootPart,
                TweenInfo.new(dist / speed, Enum.EasingStyle.Linear),
                {CFrame = CFrame.new(Mouse.Hit.p + Vector3.new(0, 3, 0))}
            ):Play()
        end
    end)
end)

-- TP to Player
TpPlayerBtn.MouseButton1Click:Connect(function()
    if _G.SelectedPlayer ~= "" then
        local target = Players:FindFirstChild(_G.SelectedPlayer)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                char.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
            end
        end
    end
end)

-- Ignore Player
IgnorePlayerBtn.MouseButton1Click:Connect(function()
    if _G.SelectedPlayer ~= "" then
        local p = Players:FindFirstChild(_G.SelectedPlayer)
        if p then
            local newState = not (_G.IgnoredPlayers[p.Name] or false)
            _G.IgnoredPlayers[p.Name] = newState
            _G.IgnoredPlayers[p.DisplayName] = newState
            _G.IgnoredPlayers[tostring(p.UserId)] = newState
        else
            _G.IgnoredPlayers[_G.SelectedPlayer] = not _G.IgnoredPlayers[_G.SelectedPlayer]
        end
        RefreshPlayers()
    end
end)

-- Infinite Jump
UserInputService.JumpRequest:Connect(function()
    if _G.InfJump and LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

-- Keybind Shortcuts (T for Auto Aim)
local _autoAimDebounce = false
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
    if UserInputService:GetFocusedTextBox() ~= nil then return end

    if input.KeyCode == Enum.KeyCode.T then
        -- Prevent repeat-key and double-fire
        if _autoAimDebounce then return end
        _autoAimDebounce = true

        ToggleAutoAim()

        task.delay(0.15, function()
            _autoAimDebounce = false
        end)
    end
end)

-- Character Added Handler for Reach
LocalPlayer.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    if _G.ReachEnabled then
        CreateReachPart()
    end
end)

-- [[ AIMBOT & FOV RENDER LOOP ]] --
RunService.RenderStepped:Connect(function(dt)
    if _G.MovementLoopID ~= CurrentLoopID then return end
    if _G.ShowFOV then UpdateFOVCircle() end
    if _G.AutoAim then RunAutoAim(dt) end
    if _G.AutoShoot then RunAutoShoot() end
end)

-- [[ MAIN LOOP ]] --
RunService.Stepped:Connect(function()
    if _G.MovementLoopID ~= CurrentLoopID then return end

    if _G.FullBright then
        Lighting.Brightness = 2
        Lighting.ClockTime = 14
        Lighting.FogEnd = 100000
        Lighting.GlobalShadows = false
        Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
    end

    -- Kill Aura tick (throttled: every 3rd frame)
    if _G.KillAura then
        if not _G._killAuraTick then _G._killAuraTick = 0 end
        _G._killAuraTick = _G._killAuraTick + 1
        if _G._killAuraTick >= 3 then
            _G._killAuraTick = 0
            RunKillAura()
        end
    end

    -- ESP (throttled: refresh every 10 frames (~6x/sec) for wall occlusion checks)
    if _G.EspEnabled then
        if not _G._espTick then _G._espTick = 0 end
        _G._espTick = _G._espTick + 1
        if _G._espTick >= 10 then
            _G._espTick = 0
            RefreshAllESP()
        end
    end

    -- Skip character logic entirely if all character-modifying features are disabled
    if not (_G.Noclip or _G.SpeedEnabled or _G.Fly or _G.ReachEnabled) then return end

    local char = LocalPlayer.Character
    if char and char:FindFirstChild("Humanoid") then
        -- Noclip (cached parts — only rescans when character changes)
        if _G.Noclip then
            if _G._noclipChar ~= char then
                _G._noclipChar = char
                _G._noclipParts = {}
                for _, v in pairs(char:GetDescendants()) do
                    if v:IsA("BasePart") then
                        table.insert(_G._noclipParts, v)
                    end
                end
            end
            for _, v in pairs(_G._noclipParts) do
                if v and v.Parent then
                    v.CanCollide = false
                end
            end
        end

        -- Walk Speed (only modify WalkSpeed when Speed feature is active)
        if _G.SpeedEnabled and not _G.Fly then
            char.Humanoid.WalkSpeed = _G.MovementSpeed
        end

        -- Fly
        if _G.Fly then
            char.Humanoid.PlatformStand = true
            local hrp = char.HumanoidRootPart
            local bv = hrp:FindFirstChild("VelocityGrip") or Instance.new("BodyVelocity", hrp)
            local bg = hrp:FindFirstChild("GyroGrip")     or Instance.new("BodyGyro",     hrp)
            bv.Name = "VelocityGrip"
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bg.Name = "GyroGrip"
            bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)

            local dir = char.Humanoid.MoveDirection
            if dir.Magnitude == 0 then
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
            end

            local flySpeed = _G.SpeedEnabled and _G.MovementSpeed or 20
            bv.Velocity = dir * flySpeed
            bg.CFrame = Camera.CFrame
        end

        -- Reach - Update tool hitboxes when equipped
        if _G.ReachEnabled then
            ExpandToolHitbox()
            UpdateReachSize()
        end
    end
end)

-- Initial animation
Main.BackgroundTransparency = 1
Main.Size = UDim2.new(0, 360, 0, 0)
CreateTween(Main, {BackgroundTransparency = 0, Size = UDim2.new(0, 360, 0, 480)}, 0.4, Enum.EasingStyle.Back):Play()
